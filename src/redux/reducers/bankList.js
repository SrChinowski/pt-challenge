const initialState = {
    bankName: "",
    description: "",
    age: 0,
    url: ""
} 

export default function(state = initialState, action ){
    switch(action.type){
        default:  return state;
    }
}